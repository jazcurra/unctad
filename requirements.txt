streamlit
pandas
python-dotenv  # For managing environment variables (API keys, DB credentials)
pdfminer.six  # For extracting text from PDFs
PyMuPDF       # Alternative PDF extraction library (more robust)
bcrypt        # For password hashing
SQLAlchemy    # For database interaction (if using)